# 开发路线图

## M0：地图 API 能力验证

- 确认地点搜索、地理编码和逆地理编码能力
- 确认公共交通站点、线路、方向和运营时间的数据范围
- 确认公共交通路径规划、矩阵或等时圈能力
- 确认配额、QPS、计费、缓存限制与服务条款
- 确认坐标系、覆盖城市和跨城行为
- 用同一组样例请求验证通勤总时间口径

完成标准：形成供应商能力表，并确定可达性算法采用直接等时圈、批量路径规划或公交网络图中的哪一种。

## M1：基础地图 MVP

状态：已完成首个可运行版本。

- 初始化 monorepo、代码规范和环境配置
- 完成地点搜索、候选选择和地图定位
- 完成附近公共交通站点查询与去重
- 展示站点类型、距离与地图位置
- 接入错误归一化和基础超时重试

当前已完成地图加载、地点提示、POI 定位、通勤条件输入、附近站点查询、错误归一化和响应式界面。站点途经线路、方向和首末班信息放到后续独立详情能力中。

完成标准：用户能够选定目的地，并查看附近公共交通站点的可靠信息。

## M2：可达性 MVP

状态：已完成第一版同步抽样计算，继续增强中。

- 支持通勤方向、出发时间、时间预算、最大步行和最大换乘设置
- 创建异步可达性任务并展示计算进度
- 计算所有候选站点的最佳公共交通路线
- 输出可达站点、地理最远站点、时间边界站点和推荐路线
- 在地图上按通勤时间分层着色

当前实现以地点为中心在 8 个方向生成采样点，每个方向优先挑选轨道交通候选站点；服务端按受控速率逐个调用高德公交路径规划，筛选预算内站点，并返回最远可达点和未达标的最近候选。结果缓存 5 分钟，地图以分钟标记展示可达站点。后续需补充分层候选、完整进度任务、最大步行/换乘条件和路线详情。

已记录的优先改进：当前 `cost.duration` 已包含高德公交方案内部的步行时间，但候选点仍以交通站点为起终点，尚未计算“住宅小区到上车站”的接驳步行，也没有最大步行时间筛选。接入小区数据时需要改为门到门口径，并单独汇总、展示和限制步行时间。

完成标准：给定地点和条件后，可以稳定复现并解释每个站点为何可达或不可达。

## M3：性能与准确性

- 加入 Redis 缓存、请求合并和并发控制
- 优先使用供应商的矩阵或等时圈能力
- 条件允许时构建站点线路图并运行时间依赖最短路径算法
- 使用路径规划 API 复核边界站点
- 增加早晚高峰、工作日和周末对比

完成标准：目标城市常见查询在配额内完成，结果与地图供应商原生规划基本一致。

## M4：租房辅助

- 查询可达站点周围的住宅小区
- 加入步行距离、租金、面积、户型和配套筛选
- 提供通勤时间、换乘、步行和价格综合评分
- 支持收藏、比较、多目的地交集和结果分享
- 视数据授权情况接入房源平台

完成标准：用户可以从通勤范围进一步得到可操作的小区或房源候选列表。
