# 开发路线图

## M0：地图 API 能力验证

- 确认地点搜索、地理编码和逆地理编码能力
- 确认公共交通站点、线路、方向和运营时间的数据范围
- 确认公共交通路径规划、矩阵或等时圈能力
- 确认配额、QPS、计费、缓存限制与服务条款
- 确认坐标系、覆盖城市和跨城行为
- 用同一组样例请求验证通勤总时间口径

完成标准：形成供应商能力表，并确定可达性算法采用直接等时圈、批量路径规划或公交网络图中的哪一种。

## M1：基础地图 MVP

状态：已完成首个可运行版本。

- 初始化 monorepo、代码规范和环境配置
- 完成地点搜索、候选选择和地图定位
- 完成附近公共交通站点查询与去重
- 展示站点类型、距离与地图位置
- 接入错误归一化和基础超时重试

当前已完成地图加载、地点提示、POI 定位、通勤条件输入、附近站点查询、错误归一化和响应式界面。站点途经线路、方向和首末班信息放到后续独立详情能力中。

完成标准：用户能够选定目的地，并查看附近公共交通站点的可靠信息。

## M2：可达性 MVP

状态：已完成第一版同步抽样计算，继续增强中。

- 支持住所到公司的出发时间、时间预算、最大步行和最大换乘设置
- 创建异步可达性任务并展示计算进度
- 计算所有候选站点的最佳公共交通路线
- 输出可达站点、地理最远站点、时间边界站点和推荐路线
- 在地图上按通勤时间分层着色

当前实现从用户选定接驳站的公交、地铁和有轨电车线路出发，调用公交线路查询取得线路两个运行方向的完整站序并生成候选；服务端先调用高德步行规划得到公司地点与接驳站之间的时间 1，再用总预算减去时间 1 得到公共交通预算时间 2，只核验“候选住所 → 公司”路线。结果缓存 5 分钟，地图绘制当前选中候选站与接驳站之间的路线；每条乘车线路单独着色，上车、途经、换乘和下车站逐站标注，换乘步行段使用虚线。点击列表会切换并高亮对应完整路线。后续需增加一层换乘线路扩展、完整进度任务和最大换乘条件。

2026-09-10 产品取舍：暂时移除“公司 → 候选住所”回程核验，只保留租房筛选最核心的上班方向。候选规模不变时，公共交通路径规划调用量由最多 24 次降为最多 12 次；未来需要晚高峰或回程差异分析时再以独立开关恢复。

2026-09-10 城市兼容修正：线路匹配统一忽略“地铁”“轨道交通”“轨交”等供应商前缀，解决武汉站点显示“2号线”但线路接口返回“轨道交通2号线”而无法生成候选的问题；有轨电车类型同时结合站名和途经线路识别。

已完成范围与计算约束优化：附近站点支持 500 米、1 公里、1.5 公里三档；用户可以选择最多 3 个公司侧接驳站点，并可为每个站点指定允许的线路。不指定线路表示允许该站全部线路。服务端按接驳站公平选择最多 8 条待展开线路，并从高德返回的备选方案中筛选包含对应指定线路的路线。

当前接驳步行已经使用高德步行路径规划，不再采用直线距离换算；`cost.duration` 继续用于公共交通方案内部的乘车、等待、换乘和步行时间。候选点仍以交通站点为起终点，接入小区数据后还需要补充“住宅小区到候选站”的另一端步行，形成完整门到门口径。

2026-09-10 本机记忆：保存最近 5 个工作地点与通勤条件，附近站点按“地点 + 半径”最多缓存 6 组，完整通勤结果按精确查询条件最多缓存 4 次。站点缓存 24 小时、通勤结果 2 小时内可直接复用以减少高德 API 调用；两类记录最多保留 7 天作为过期提示和失败兜底。用户可关闭记忆或一键清空，地图密钥不进入浏览器存储。

2026-09-11 多接驳站结果修正：路线结果由“候选住所站”去重改为按“候选住所站 + 公司侧接驳站”分别保留，避免多个已选接驳站的路线互相覆盖。结果按接驳站分组展示，地图默认同时绘制各接驳站的最远代表路线；点击任一路线后保持该路线高亮，其他接驳站路线以浅色作为对照。

2026-09-11 首次使用初始化：新增独立的首屏设置流程，依次收集工作地点、通勤预算、出发时间和本地记忆选择。已有本机工作地点记录时自动恢复并进入地图；主页面保留“重新设置”，首次使用也可暂时跳过，避免初始化成为阻塞入口。

2026-09-11 本地开发初始化：新增仅监听本机回环地址的浏览器初始化助手。下载代码并安装 Node.js 后，Windows 用户可双击启动文件，在界面中填写或复用高德配置，由助手自动安装依赖并启动应用；密钥只写入 Git 已忽略的 `.env.local`，不进入线上初始化页面。

2026-09-11 本地启动容错：初始化助手会保留安装和启动日志，首次启动失败时自动重新安装依赖并重试；如果检测到同一项目已经在本机运行，则直接复用已有地址，不再把开发服务的退出码 1 误报为应用失败。

2026-09-12 本地配置识别：初始化助手同时检测项目根目录和 `apps/web` 下的 `.env.local`，兼容 UTF-8 BOM、引号及 `export` 写法，并按字段显示“已识别/待填写”。已识别的密钥无需重复输入且不会在页面回显；位于 `apps/web` 的配置会在启动时安全合并到项目实际读取的根目录文件。

2026-09-12 多站多方向计算修正：移除所有已选接驳站共用候选池并互相组合的旧逻辑，改为按“接驳站 + 指定线路 + 高德线路方向”独立解析有序站表。住所到公司的候选只取接驳站在该运行方向上的上游站点；每个方向先扣除公司到接驳站的步行时间，再以剩余公共交通时间进行边界搜索和路径规划复核。单个中间地铁站通常显示两个方向，多选站点时也分别保留各自方向；若接驳站是某方向起点，则明确记录该方向没有上游候选。地图同步显示所有可达方向，列表标注具体线路与开往终点。

完成标准：给定地点和条件后，可以稳定复现并解释每个站点为何可达或不可达。

## M3：性能与准确性

- 加入 Redis 缓存、请求合并和并发控制
- 优先使用供应商的矩阵或等时圈能力
- 条件允许时构建站点线路图并运行时间依赖最短路径算法
- 使用路径规划 API 复核边界站点
- 增加早晚高峰、工作日和周末对比

完成标准：目标城市常见查询在配额内完成，结果与地图供应商原生规划基本一致。

## M4：租房辅助

- 查询可达站点周围的住宅小区
- 加入步行距离、租金、面积、户型和配套筛选
- 提供通勤时间、换乘、步行和价格综合评分
- 支持收藏、比较、多目的地交集和结果分享
- 视数据授权情况接入房源平台

完成标准：用户可以从通勤范围进一步得到可操作的小区或房源候选列表。
